model Country {
  id        Int    @id @default(autoincrement())
  name      String @unique
  code      String @unique
}

model Hobby {
  id   Int    @id @default(autoincrement())
  name String @unique
}